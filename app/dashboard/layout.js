export const metadata = {
  title: "Dashboard | Get Me A Chai",
  description: "Track your earnings, supporters, and creator stats.",
};

export default function DashboardLayout({ children }) {
  return <>{children}</>;
}
